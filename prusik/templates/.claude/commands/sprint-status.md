---
description: Show the active sprint's phase, feature, writable patterns, budget, and exit artifact status.
allowed-tools: [Bash]
---

Run `prusik status` via Bash and show the output. That's it — nothing else to do.

If the user asks follow-up questions about how to proceed, consult the phase spec in `.claude/sprint-config.yaml` and the command guides in `.claude/commands/`.
