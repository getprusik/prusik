---
description: Re-engage Stop-hook exit-artifact enforcement after /sprint-pause.
allowed-tools: [Bash]
---

Run `prusik resume` via Bash and show the output.

Use after `/sprint-pause` when you're ready for the Stop hook to enforce phase exit artifacts again. Typical lifecycle: pause to yield mid-phase → user gives input → resume → continue toward exit artifacts.
