# Product-fit acknowledgement — <feature>

Co-authored by the operator + live-cc when the brief is written. This is the
evidence that this feature was designed with the whole product in view. Prusik
verifies every reference below resolves against `design/product.md` and the
existing feature set — a claim that doesn't resolve blocks sprint-start.

## Advances

<!-- Which product pillar(s) from design/product.md this feature advances.
     Name real pillars (id or name). At least one. -->
- P1 — <how this feature advances that pillar>

## Related

<!-- Prior features this reconciles with, and how (extends / depends-on /
     overlaps / supersedes). Each cited feature must be an existing
     briefs/<slug>.md. State "none" only if this is genuinely the first feature
     in its area. -->
- <feature-slug>: <relationship>

## Concepts

<!-- Domain terms this feature touches. Reuse the canonical term [canonical], or
     register a genuinely new one [new: <definition>]. You cannot silently
     introduce a second name for an existing concept. -->
- <term> [canonical]
- <term> [new: <one-line definition>]
